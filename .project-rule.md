# Project Configuration & Development Standards (ExportEngine)

Tên dự án hiện tại: **ExportEngine**

---

## 1. Giới thiệu & Mục tiêu
`ExportEngine` cung cấp Fluent API mạnh mẽ để xuất dữ liệu từ các danh sách đối tượng sang CSV hoặc Excel (.xlsx) với hiệu năng tối ưu.

Các tính năng cốt lõi:
- **Tự động nhận diện cột:** Tự động phát hiện các cột dựa trên thuộc tính public của đối tượng hoặc cho phép định cấu hình thủ công thông qua `.Column("Header", x => x.Prop)`.
- **Định dạng dữ liệu:** Áp dụng định dạng hiển thị cho ngày tháng, số tiền trực tiếp vào tệp Excel/CSV.
- **Trình bày Excel đẹp mắt:** Tự động áp dụng tiêu đề, màu nền header xanh lam, dải màu xen kẽ (zebra striping), bộ lọc tự động (auto-filter) và tự động căn chỉnh độ rộng cột (auto-width).
- **Hỗ trợ nhiều sheet:** Cho phép xuất nhiều tập dữ liệu vào các sheet khác nhau trên cùng một file Excel bằng `MultiExport`.
- **Hỗ trợ Stream:** Cho phép ghi trực tiếp ra File, Stream hoặc trả về Byte Array (hữu ích cho WebAPI).

---

## 2. Quy chuẩn Phát triển & Kiến trúc
- **Fluent API:** Tuân thủ chuỗi lệnh gọi liên tiếp để cấu hình dữ liệu xuất.
- **Không phụ thuộc thư viện nặng:** Sử dụng các thư viện gọn nhẹ và tối ưu hóa việc sử dụng tài nguyên (Stream) để tránh rò rỉ bộ nhớ khi xuất file dung lượng lớn.
- **Thống nhất định dạng:** Luôn định rõ các ký tự phân tách (delimiter) khi xuất CSV để tránh lỗi hiển thị font hoặc sai cột trên các máy tính khác nhau.

---

## 3. Lệnh Hữu ích
*   **Build dự án:**
    ```bash
    dotnet build src/ExportEngine/ExportEngine.csproj -c Release
    ```
